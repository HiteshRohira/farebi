# Created by `pnpm dev:backend` after connecting a Convex deployment.
CONVEX_DEPLOYMENT=
VITE_CONVEX_URL=
VITE_CONVEX_SITE_URL=

# Public origin used by the Vite app. The matching SITE_URL is configured on
# the Convex deployment; Better Auth server secrets do not belong in this file.
VITE_SITE_URL=http://localhost:3000
